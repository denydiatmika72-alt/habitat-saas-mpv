{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.js",
  "scripts": {
    "start": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 NODE_OPTIONS=--dns-result-order=ipv4first node src/index.js",
    "dev": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 NODE_OPTIONS=--dns-result-order=ipv4first nodemon src/index.js",
    "migrate": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 NODE_OPTIONS=--dns-result-order=ipv4first npx prisma migrate dev",
    "prisma:generate": "npx prisma generate",
    "prisma:studio": "cross-env NODE_TLS_REJECT_UNAUTHORIZED=0 NODE_OPTIONS=--dns-result-order=ipv4first npx prisma studio",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@prisma/adapter-pg": "^7.8.0",
    "@prisma/client": "^7.8.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.14",
    "prisma": "^7.8.0"
  }
}
